cmake_minimum_required(VERSION 3.12)

include_directories(${PSim_SOURCE_DIR}/lin/include)
include_directories(${PSim_SOURCE_DIR}/include)

set(This PSim_sources)

set(Sources
# TODO once we have sources
)
