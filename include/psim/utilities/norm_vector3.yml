
name: NormVector3Interface
type: Model
comment: >
    Calculates the norm of a three dimensional vector.

args:
    - vector

adds:
    - name: "{vector}.norm"
      type: Lazy Real
      comment: >
          Magnitude of the vector.

gets:
    - name: "{vector}"
      type: Vector3
