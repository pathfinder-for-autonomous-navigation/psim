function [nstime] = nsgpstime(self)
%NSGPSTIME Returns the time of the orbit in int64 ns since GPS epoch.
%grav calls: 0
%args:
%   self(not propagating valid Orbit): The Orbit.
nstime= int64(0);
end

