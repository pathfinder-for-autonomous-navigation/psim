function [self,num_grav_calls] = startpropagator(self,dt_ns,earth_rate_ecef)
%STARTPROPAGATOR Puts the orbit in propagator mode, call finishpropagator
% or onegravcall num_grav_calls times to finish the propagation.
num_grav_calls=0;
end

