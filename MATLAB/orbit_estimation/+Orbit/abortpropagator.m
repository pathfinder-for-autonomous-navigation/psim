function [self] = abortpropagator(self)
%ABORTPROPAGATOR Aborts the propagator,
%   If self is propagating this restores self to before
%   startpropagator was called. If self is not propagating, this does
%   nothing.
end

