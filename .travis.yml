matrix:
  include:
    - language: python
      python: 3.6
      before_install:
        - export CI
      install:
        - pip install -U platformio
        - platformio update
      script:
        - platformio test --verbose --environment ci_github
