matrix:
  include:
    - language: python
      python: 3.7
      before_install:
        - export CI
      install:
        - python -m venv venv
        - source venv/bin/activate
        - pip install -r requirements.txt
        - pio platform install native
      script:
        - platformio test --verbose --environment github
